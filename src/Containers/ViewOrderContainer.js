/* eslint-disable prettier/prettier */
import { connect } from 'react-redux';
import ScreenView from '../Screens/Order/ScreenViewOrder';
const mapStateToProps = state => ({

})
const mapDispatchToProps = {
}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenView)